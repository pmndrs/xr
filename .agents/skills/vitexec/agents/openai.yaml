interface:
  display_name: "vitexec"
  short_description: "Run snippets inside Vite apps"
  default_prompt: "Use $vitexec to inspect a live Vite app by running a snippet and reading browser logs; for WebXR, use IWER and prove an active XR session."
